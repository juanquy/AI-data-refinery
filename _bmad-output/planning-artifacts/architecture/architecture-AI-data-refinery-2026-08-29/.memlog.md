---
scope: Universal Data Refinery platform (whole system, launch scope per PRD 2026-08-29)
purpose: Build-substrate architecture spine derived from launch PRD + prototype repo; plus co-founder walkthrough artifact
altitude: initiative→features
updated: 2026-08-29T17:00
---

- (decision) Working mode: Coaching path; deliverables = ARCHITECTURE-SPINE.md (build substrate) + co-founder walkthrough artifact (audience: co-founder alignment on technical direction and trade-offs)
- (constraint) Binding source: launch PRD prd-AI-data-refinery-2026-08-29 (FR-001..101, NFR-001..051) + its memlog; PRD settles surfaces (MCP+REST core, Studio, SDKs; extension/CLI post-launch), dual billing rails, two-part pricing units, marketplace accrual+fraud controls, quality-gated launch — inherit silently, do not re-decide
- (constraint) Prototype repo is reference/starting-point, NOT product boundary (PRD directive); AI-generated by AntiGravity IDE; driver is 'proper engineering and audit' — conventions need ratification or overturning, not blind adoption
- (event) Explore subagent dispatched to sweep prototype (stack+versions, topology, data layer, mutation/async, API surfaces, conventions, quality signals)
- (event) Code sweep complete; digest saved to code-sweep.md. Stack as-built: CF Workers (wrangler 3, single env), Hono 4.13, Zod 3.25, React 18 SPA, npm workspaces; bindings D1+KV+Vectorize+Workers AI only — no R2/DO/Queues/Workflows, no MCP SDK, no Stripe SDK, no ORM, no CI, 7 pure-function tests
- (constraint) Sweep verdict: prototype has NO async execution substrate (refinement is request-synchronous; cron selects pipelines then discards them), NO enforceable auth (3 ad-hoc schemes, founder passcodes hardcoded in source+DB, any Pro key = platform admin), NO tenant columns on data tables, unvalidated inbound HTTP, split schema ownership (SQL DDL in worker migrations vs Zod in packages/schema, unlinked). Internals must be rebuilt regardless of platform choice
- (question) OPEN (posed to user): ratify Cloudflare as platform vs open re-decision vs hybrid — sweep shows rewrite tax is paid either way, so the fork reduces to 'is CF the right platform for this product?'
- (decision) AD candidate [ADOPTED]: Platform = Cloudflare (Workers + platform primitives) — founder's own choice, ratified by user after sweep evidence; hybrid/external compute is NOT day-one, may only appear later as a named escape hatch if pipeline outgrows Workers limits
- (direction) Next coaching forks queued: (1) pipeline async substrate (Queues vs Workflows vs DO alarms) — verify current CF offerings on web first; (2) design paradigm + data-access/schema-ownership stance; (3) tenancy + auth model; (4) environments/CI operational envelope
- (version) Web-verified 2026-08-29: CF Workflows GA/V2 (50k concurrent instances Paid, 300/s creation, 500k steps/mo incl then $0.80/100k, 1MiB/step result); Queues (250 concurrent push consumers, batch 100, 5k msg/s/queue, 24h delay, DLQ); Workers Paid CPU configurable to 5min/invocation (default 30s, network wait excluded); D1 max 10GB/database; Agents SDK McpAgent/createMcpHandler = recommended MCP path, OAuth 2.1 + MCP spec 2026-07-28, Streamable HTTP
- (direction) Substrate lean to pose: Workflows = durable refinement runs (per-step retry fits fetch→extract→validate→diff→store), Queues = fan-out + outbound webhook delivery; on-demand stays in-request but shares one refinement core (one core, two entry modes). Alternatives: Queues-only (simpler, whole-run retry), DO alarms (hand-rolled, rejected lean)
- (decision) AD candidate: Execution substrate = CF Workflows (one instance per scheduled refinement run; per-step durability/retry over fetch→sanitize→extract→validate→diff→store) + Queues (dispatch fan-out from cron, outbound webhook delivery w/ retries+DLQ). On-demand refinement stays synchronous in-request sharing the SAME refinement core — one core, two entry modes, no third path. Rejected: Queues-only (whole-run retry waste), DO alarms (hand-rolled substrate)
- (decision) AD candidate: Schema ownership = Drizzle ORM as single source of truth in packages/schema (TS schema → generated D1 migrations + row types; drizzle-zod → Zod validators). Kills split-brain DDL-vs-Zod bug class; typed queries replace ~40 'as any' casts. Rejected: raw SQL + generated types (keeps parallel artifacts in sync by hand)
- (version) Web-verified 2026-08-29: Drizzle ORM + drizzle-kit officially support D1 (d1-http driver; drizzle-kit generate → wrangler d1 migrations apply); stripe-node supported on Workers via Stripe.createFetchHttpClient() (official stripe-samples template); x402 = Linux Foundation standard since 2026-04 (V2: session tokens, multi-chain; TS SDK Apache-2.0; Coinbase+Cloudflare founders; 69k active agents / $50M cumulative volume by 2026-04; MCP integration direction). Exact npm versions to pin at stack-seed time
- (decision) AD candidate: Data ownership = two classes. Platform corpus (public verticals: refined_entities+diffs, refined once, served to all) vs tenant resources (workspaces, keys, custom schemas, pipelines, webhooks, listings, wallets, usage events — every row carries workspace_id). Custom-schema refinement outputs are PRIVATE to owning workspace by default (schema = tenant IP); sharing only via explicit marketplace listing. KV keys + Vectorize metadata carry same owner dimension. RULE: no query path reads either class without explicit owner scope. Rejected: all-shared corpus (leaks tenant IP), per-schema configurability (consent/pricing complexity at launch)
- (decision) AD candidate: Identity = 4 principal types, one scheme each — human user (Studio session via Better Auth on D1/Drizzle), workspace API key (hashed at rest, scoped), agent token (workspace-bound, wallet+kill-switch), drive-by x402 agent (payment proof is the credential, no account). Platform admin = role on user account, NEVER derived from plan/key type; founder passcodes eliminated. MCP auth via CF Agents SDK: OAuth 2.1 user-delegated + API-key/x402 direct. Better Auth caveats fenced: avoid cookieCache+secondaryStorage combo (bug #4203), per-request config instantiation on workerd. Rejected: managed IdP (external dependency in login path), hand-rolled (contradicts audit driver)
- (version) Web-verified 2026-08-29: Workers static assets = CF-recommended for new frontends (Pages in maintenance mode); @hono/zod-openapi v1.6.1 = most-used OpenAPI+validation route on Hono (chanfana = CF alternative, rejected: zod-openapi has broader community/docs)
- (direction) Final frame proposed to user for ratification: hexagonal modular monolith (core/adapters/entry, single worker), contract-first API via zod-openapi, strict validation both boundaries (LLM failure=quarantined run not fallback), single error envelope, append-only usage_events metering (single write path), R2 for raw snapshots, dev/staging/prod wrangler envs + GH Actions CI, vitest-pool-workers, npm workspaces kept, SPA decomposed (react-router+TanStack Query, typed client from OpenAPI, pure public-API client)
- (decision) User ratified full final frame verbatim (paradigm: hexagonal modular monolith single worker; contract-first zod-openapi; strict two-boundary validation; single error envelope; usage_events single-path metering; R2/D1/KV/Vectorize topology; 3 wrangler envs + GH Actions CI + vitest-pool-workers; npm workspaces kept; SPA decomposed react-router+TanStack Query). No objections, no extra dimensions requested. Entering Finalize
- (version) Stack pins (npm registry 2026-08-29): hono 4.13.5, zod 4.5.4 (REQUIRED by @hono/zod-openapi 1.6.1 peer zod ^4; drizzle-zod 0.8.3 accepts ^4), drizzle-orm 0.45.2, drizzle-kit 0.31.10, drizzle-zod 0.8.3, better-auth 1.7.2, stripe 22.6.0, x402-hono 1.2.0, agents 0.22.0, @modelcontextprotocol/sdk 1.30.0, wrangler 4.127.1 (upgrade from proto v3), vitest 4.1.11, @cloudflare/vitest-pool-workers 0.22.0, react-router 7.18.3 (v8 needs React>=19.2.7 — rejected, keeping React 18.3.1 ADOPTED), @tanstack/react-query 5.102.8, vite 6.4.3 ADOPTED, tailwindcss 3.4.19 ADOPTED, typescript 5.9.3 ADOPTED (TS7 deferred)
- (decision) Frontend toolchain upgrades DEFERRED post-launch (React 19, react-router 8, Vite 8, Tailwind 4, TS 7): decompose the SPA on the toolchain it runs on today; revisit after launch. Zod 3→4 adopted NOW because validation layer is rebuilt from scratch around zod-openapi anyway
- (event) Reconcile complete (reviews/reconcile-prd.md): 0 critical, 2 high, 9 medium — ALL 11 applied: new AD-14 policed fetch boundary (SSRF/robots/UA/rate-limits/opt-out registry/untrusted fencing); AD-11 extended (billable-on-success, compensating events never edits, audit_events same pattern); AD-4 (webhook signing both directions, paused state + replay); AD-6 (public surfaces = platform scope, tenant badges owner-opted); AD-7 (role matrix checked in core); AD-1 (external LLM via AI Gateway exception — deliberate decision resolving FR-004/NFR-012 vs CF-exclusivity); conventions +severity enums +no-training; ERD +AUDIT_EVENT +OPT_OUT_SOURCE, USAGE_EVENT workspace-optional; map +tenancy/roles +ops-console rows; deferred guardrails row cites FR-012. Lint clean. Dispatching reviewer gate (4 lenses)
- (event) Reviewer gate complete (reviews/review-{rubric,verification,adversarial,security}.md): rubric PASS 0C/2H/4M/4L; verification PASS-WITH-CHANGES 0C/1H/3M/3L; adversarial HOLES 6C/8H/3M; security 3C/5H/6M/1L. All criticals+highs and all actionable mediums applied in one fix pass; spine now AD-1..AD-16
- (decision) Gate-driven decisions distilled into spine: AD-15 wallet money-order (core/metering owns WALLET, atomic reserve→settle/release in one D1 batch, agent tokens draw + keys only via FR-067 top-up, kill = settle-prior/no-new-holds, x402 VERIFY-ONLY pre-serve + settle-post-delivery + never-settle-on-failure eliminating anonymous refunds); AD-16 custom-schema canonical doc (constrained JSON Schema profile + meta-schema in packages/schema, ONE compiler → runtime validator + MCP inputSchema + OpenAPI components, severity rules attach to doc); AD-11 row contract (price frozen at serve in micro-USD + pricing-config version + lst_ ref/split; classification by one core/metering function from serve descriptor; export = N cached_read; delivery-synchronous append; payment evidence single-use); AD-6 write authority (platform rows only via platform pipeline runs; anonymous x402 output never persisted as tenant/platform; branded OwnerScope minted only by core/tenancy; 3 derived scopes: grant/admin/unions; Vectorize owner scope = namespace)
- (version) Gate re-pins/verifications 2026-08-29: x402-hono 1.2.0 REJECTED (zod ^3 peer conflicts with zod 4.5.4; superseded v1 line) → @x402/hono 2.24.0 (+@x402/core,@x402/paywall; no zod peer; matches agents@0.22.0 optional peers @x402/core^2); added @modelcontextprotocol/client+server 2.0.0 (non-optional agents peers); vitest-pool-workers 0.22.0↔vitest 4.1.11 OK (peer ^4.1.0); better-auth 1.7.2↔drizzle-orm 0.45.2 OK (adapter in-box); compat_date≥2026-08-04 nodejs_compat default confirmed vs CF changelog; AI Gateway external providers confirmed (23+ incl OpenAI/Anthropic, unified API since 2026-05); Rate Limiting binding GA but per-colo 10/60s only → split convention (inbound abuse only; crawl pacing = D1 next_fetch_at); Vectorize metadata index NOT retroactive → owner scope = namespace. CORRECTION: x402 Foundation operationally live under Linux Foundation 2026-07-14 (not 2026-04)
- (event) Co-founder walkthrough 'Refinery Blueprint' built (walkthrough.html) and published as private artifact https://claude.ai/code/artifact/bf895fae-244f-4ada-9ccf-f3725ac676d0 — 10 sections, 3 SVG mechanism diagrams, AD-1..16 interactive index, 4 countersign questions; doc_standards pass (bmad-review structure→prose) running
- (event) doc_standards pass complete on walkthrough (bmad-review structure→prose): structure 7 findings (2 preserve) — countersign block promoted to lead s09, cover thesis + linked four calls added, AD-1/6/15 tagged countersign, gain/fence false-pair table split into two lists, section-07 naming aligned; prose 13+2 fixes applied (garden-path in call 3, three-fences count, comma splices, terminology: tripwire/code-sweep). Republished artifact same URL, label polished-v2
- (event) spine finalized
